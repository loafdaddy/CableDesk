# CableDesk direct-link kernel module.
#
# Left empty/commented for this milestone: cabledesk-helper does not load
# any module yet (see crates/cabledesk-helper — PrepareDirectLink is not
# implemented). Per docs/NETWORKING.md, thunderbolt-net may also load
# automatically as a dependency of the base "thunderbolt" driver when a
# peer is detected, so unconditionally loading it at boot on every
# CableDesk install may be unnecessary or even wrong on hardware without a
# Thunderbolt/USB4 controller at all. This file is intentionally shipped
# empty until that is settled — see docs/OPEN_QUESTIONS.md.
#
# thunderbolt-net
